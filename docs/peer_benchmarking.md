# Peer benchmarking

Peers use sector, cost bucket, project-age bucket and physical-progress stage; ministry is added only when the remaining cohort is large enough. The focal project is always excluded and at least five peer projects are required. Results include median progress, expenditure ratio and risk plus bounded percentiles. They are descriptive comparisons, not causal benchmarks.
