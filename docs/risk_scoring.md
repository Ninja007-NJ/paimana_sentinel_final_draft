# Risk scoring

Delay probability comes only from the frozen calibrated V1.1 artifact. LOW, MEDIUM, HIGH and CRITICAL bands use its frozen threshold configuration; no threshold was selected from product test traffic. Risk is a predictive signal, not a statement that delay is certain.

Early warnings are separate deterministic rules. Ranking combines current risk, recent increase, severity, project cost and data reliability. Alert priority does not alter probability.
